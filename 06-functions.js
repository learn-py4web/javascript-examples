function addone(x) {
  return x + 1
};

console.log(addone(1)); // 2

console.log(some_function(10));

function some_function(x) {
    let y = x + 1;
    let z = x - 1;
    return Math.sqrt(y * z);
}
